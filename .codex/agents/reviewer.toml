model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Follow the Reviewer role and Delegation context in the repository root AGENTS.md. Apply its task-scoped reading and RFC lookup policy; do not preload RFC documents or their index."
