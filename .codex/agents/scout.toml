model = "gpt-6-sol"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
developer_instructions = "Follow the Scout role and Delegation context in the repository root AGENTS.md. Apply its task-scoped reading and RFC lookup policy; do not preload RFC documents or their index."
