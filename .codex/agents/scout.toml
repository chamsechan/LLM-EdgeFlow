model = "gpt-6-sol"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
developer_instructions = "Follow the Scout role and Delegation context in the repository root AGENTS.md. Apply its task-scoped reading policy; start with current guides and affected code/tests."
