model = "gpt-6-astra"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = "Follow the Mechanical worker role and Delegation context in the repository root AGENTS.md. Apply its task-scoped reading and RFC lookup policy; do not preload RFC documents or their index."
