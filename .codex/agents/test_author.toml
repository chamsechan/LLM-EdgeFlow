model = "gpt-6-sol"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = "Follow the Test author role and Delegation context in the repository root AGENTS.md. Apply its task-scoped reading and RFC lookup policy; do not preload RFC documents or their index."
