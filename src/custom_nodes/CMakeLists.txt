# List custom node translation units here. They use the same Layer 3 target
# and registration path as common nodes, including SDK, CLI and Demo consumers.
target_sources(edgeflow_layer3_node_objects PRIVATE
)
